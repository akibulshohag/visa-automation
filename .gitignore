node_modules/
.env
paymentConfig.js
/.idea
uploads/
yarn.lock

# Locally-downloaded IVAC bundle cache (fetched fresh from live; never commit) + probe output
last-bundle.local.js
last-bundle.local.json
probe-capture.json

# Full DB export (real account passwords, access tokens, proxy credentials) — never commit
full_db_export.sql